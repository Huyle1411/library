#include "bits/stdc++.h"
int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);
  int T;
  std::cin >> T;
  for (int tc = 1; tc <= T; ++tc) {
    std::cout << "Case #" << tc << ": ";
  }
}
